[tool.poetry]
name = "repl_python3_project6-jschu87"
version = "0.1.0"
description = ""
authors = ["jschu87 <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
discord = "^1.0.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
